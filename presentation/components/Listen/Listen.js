// @flow
// eslint-disable-next-line no-unused-vars
import React, { Component } from 'react';
import { css } from 'aphrodite';

import styles from './Listen.styles';


const Listen = () => {
  return (
    <div className={css(styles.listen)}>
      Your Component Here :)
    </div>
  );
};

Listen.defaultProps = {

};

export default Listen;
